void input_matrix(int a[][100], int n); 
//cách hợp lệ trong C để truyền mảng 2 chiều vào hàm 
//mà không cần khai báo kích thước dòng (số hàng) 
//nhưng phải khai báo kích thước cột (số cột).
int sum_matrix(int a[][100], int n);
void print_transpose(int a[][100], int n);