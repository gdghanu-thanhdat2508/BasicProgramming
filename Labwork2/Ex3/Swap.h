void swap_temp(int *a, int *b);         // Sử dụng biến tạm thời
void swap_add_sub(int *a, int *b);      // Sử dụng phép toán cộng và trừ
void swap_mul_div(int *a, int *b);     // Sử dụng phép toán nhân và chia